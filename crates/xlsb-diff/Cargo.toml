[package]
name = "xlsb-diff"
version = "0.1.0"
edition = "2021"

[dependencies]
xlsx-diff = { path = "../xlsx-diff" }

[dev-dependencies]
anyhow = "1.0"
formula-office-crypto = { path = "../formula-office-crypto" }
tempfile = "3.10"
