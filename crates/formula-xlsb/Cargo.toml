[package]
name = "formula-xlsb"
version = "0.1.0"
edition = "2021"

[dependencies]
quick-xml = "0.31"
thiserror = "1.0"
zip = { version = "0.6", default-features = false, features = ["deflate"] }

[dev-dependencies]
assert_cmd = "2.0"
pretty_assertions = "1.4"
tempfile = "3.10"
