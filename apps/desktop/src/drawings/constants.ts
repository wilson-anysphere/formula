/**
 * Pixel size of the resize/selection handles rendered for drawings.
 *
 * This is shared between canvas rendering (`DrawingOverlay`) and interaction hit
 * testing (`DrawingInteractionController`) so the visible handle size matches
 * the interactive region.
 */
export const DRAWING_HANDLE_SIZE_PX = 8;

